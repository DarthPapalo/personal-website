---
import { base, i18n } from "astro:config/client";

export const prerender = false;

const targetLocale = Astro.preferredLocale || i18n?.defaultLocale; 

return Astro.redirect(`${base}/${targetLocale}/`);
---